# 蚂蚁森林 现状 vs 旧脚本 版本差异调研

> 调研时间 2026-07-08｜环境：MuMu 模拟器（Android 12，伪装华为 ALN-AL1，1440×2560，Houdini ARM 翻译）｜支付宝小号已登录｜困鱼（AutoJS.fake）已安装｜工具：ADB + mobile MCP

## 一、核心架构发现

1. **蚂蚁森林主体是 H5（跑在 Nebula WebView）**，不是原生控件。但 WebView 把 H5 内容吐成了无障碍节点（带 text），所以 H5 元素**能被 text/desc 定位**——比纯图像识别更有潜力。
2. **H5 滚动列表项的无障碍 bounds 报成 (0,0)**（只有顶部固定区有真实坐标）→ 这迫使旧脚本对能量球/好友列表必须用**图像识别（霍夫/YOLO）+ 滑动翻页**，不能靠控件坐标。这个限制当前版本依旧，旧脚本的图像方案是对的。
3. **原生浮层**（标题栏、原生弹窗如 `dialog_bg`）有真实 ID/desc/bounds，可用控件定位。原生层和 H5 层要分开处理。

## 二、入口机制（验证通过 ✅）

- 深度链接 `alipays://platformapi/startapp?appId=60000002` 仍有效，经 `com.alipay.mobile.quinox.LauncherActivity` 分发进蚂蚁森林。**旧脚本的入口机制没变。**
- mobile MCP 的 `open_url` 拦截 `alipays:` 协议（只允许 http/https），改用 `adb shell am start -a android.intent.action.VIEW -d "alipays://..."`（注意 URL 加引号防设备 shell 把 `?` 当通配符）。

## 三、关键版本差异 ⚠️

### 差异 1（重大）：默认进"森林广场"（社交版），不是"个人版"
- 当前账号进蚂蚁森林默认落在 **广场版**：含森林动态流、好友排行榜（张鲁波/鸾凤和鸣 等）、小游戏入口（能量雨/神奇鱼塘/超能下蛋鸭/灵画师/赚能量/公益林/森林集市/真爱合种）、个人树+统计（收好友 3789g / 被好友收 28g）。
- 顶部有 **"切换为个人版"** 按钮（H5，中心约 720,188）。
- **旧脚本假设进的是"个人版"**（自己那棵树 + "找能量"逛好友按钮），广场版首页**看不到"找能量"按钮**。
- **修复方向**：脚本进蚂蚁森林后，需先点"切换为个人版"切到经典视图，再走原流程。（待验证：个人版里"找能量"等按钮是否还在原位）

### 差异 2：分辨率
- 模拟器 1440×2560（density 640）。旧脚本配置的能量球区域 `tree_collect_left/top/width/height`、逛一逛按钮区域 `stroll_button_*` 都需按此分辨率重算（旧配置多半基于 1080×…）。

### 差异 3（待验证）：收集流程关键文本/控件
| 旧脚本用途 | 文本 / 选择器 | 当前状态 |
|---|---|---|
| 逛好友入口按钮 | 文本 **"找能量"** | ⏳ 待验证（应在个人版） |
| 好友主页标题 | 正则 **`(.*)的蚂蚁森林`** | ⏳ 待验证 |
| 逛一逛结束标志 | **`找能量共获得.*`** | ⏳ 待验证 |
| 好友列表到底 | **`.*没有更多了.*`** | ⏳ 待验证 |
| 好友倒计时 | **`\d+'`**（如 `5'`） | ⏳ 待验证 |
| 能量雨入口 | **`.*能量雨.*`** | ✅ 广场版有"能量雨"入口 |
| 原生弹窗关闭 | `idMatches('.*(J_pop_treedialog_close\|af-mask-close).*')`、`descMatches('关闭蒙层\|开心收下\|关闭弹窗')` | ⏳ 待验证 |
| 更多菜单（原生） | `dialog_bg`，含 攻略/分享/帮助/设置 | ✅ 已验证存在 |

### 差异 4（已修复 ✅，2026-07-16）：逛一逛"好友间转场"改为向下滑动

- **现象**：新版逛一逛，收完一个好友转下一个时，原来的"点击找能量/逛一逛按钮"跳转方式失效（按钮还在但点了不跳，或按钮消失），改为**在好友主页向下滑动**，页面带你去下一个好友。
- **首次入口不变**：从自己森林/广场版进**第一个**好友，仍是点"找能量"按钮。
- **影响代码**：`core/StrollScanner.js` 的 `collecting()` 循环原第 122 行 `automator.click(逛一逛按钮中心)` —— 老脚本每轮都靠它转场，新版下不跳。
- **修复**：循环内加 `firstRound` 标志区分两种转场：
  - 首轮 → 保留 `automator.click(找能量按钮)`（循环外的 region 识别继续用）
  - 后续轮 → `automator.randomScrollDown()` 滑动转下一个（复用 `FriendListScanner` 滑好友列表的同款函数，定义在 `lib/prototype/Automator.js:164`）
- **安全网**：`collectTargetFriend` 的 `duplicateEnterCount`（连续 3 次进同一好友即退出）天然覆盖"滑动没跳过去"的情况，不会死循环。
- **待验证**：滑动后 `collectTargetFriend` 能否稳定识别新好友标题 `(.*)的蚂蚁森林`；异常重试路径（`StrollScanner.js` 第 210 行 `clickRandomRegion`）在新版下兜底失效，若频繁报"重试超过3次"需再调。

## 四、旧脚本收集流程（已读懂）

入口 `main.js` → `core/Ant_forest.js`：
1. 深链进自己森林（个人版）→ 收**自己**能量球（霍夫/YOLO 在 `tree_collect_*` 区域）。
2. 点 **"找能量"**（逛一逛，`StrollScanner`）→ 自动跳好友森林 → 标题 `(.*)的蚂蚁森林` → 收球 → 自动下一个 → 直到 `找能量共获得X`。
3. 或 `FriendListScanner`：翻好友列表，读倒计时 `\d+'` 排下次收取，直到 `没有更多了`。
4. `clearPopup()` 处理原生弹窗（上面那串 id/desc）。
5. 能量雨单独脚本 `unit/能量雨收集.js`。
6. 一键收能量 `collectByOneKeyCollect()`（`use_one_key_collect` 配置）。

## 五、调研工具的坑（备忘，避免重复踩）

- **mobile MCP `input tap` 坐标系有 bug**：输入 (720,188) 实际点 (1920,501)，放大约 2.667× → 所有点击改用 **`adb shell input tap`（设备坐标，已验证准确）**。
- **mobile MCP `ui find` 不进 WebView**（找不到 H5 元素），但 `ui tree` 能拿到完整 H5 节点 → 定位用 **tree，不用 find**。
- **BACK 键会退出整个蚂蚁森林**（深链直进、无历史栈）→ 关弹窗用**点弹窗外部**，不用 BACK。
- **H5 按钮盖在原生标题栏下时，ADB 坐标点击被原生层拦截**（"切换为个人版"就是例子）→ 这种元素需**无障碍 ACTION_CLICK（困鱼）**才能点。
- `analyze_image`（视觉模型）对支付宝截图**会幻觉**（编造不存在的 UI），不可用 → 视觉走 **`adb screencap`→本地→Read**。
- 截图经 mobile MCP / Read 会上传云端，**避免截含账号/验证码的界面**。

## 六、困鱼环境搭建实测（已完成 ✅）

困鱼（`com.taobao.idlefish.x`，伪装成闲鱼）已装好并跑通脚本，关键步骤备忘：

1. **包名伪装**：困鱼包名是 `com.taobao.idlefish.x`（闲鱼是 `com.taobao.idlefish`），无障碍服务类 `com.taobao.idlefish.AccessibilityService`，启动 Activity `org.autojs.autojs.ui.splash.SplashActivity`，脚本执行 `org.autojs.autojs.external.open.RunIntentActivity`。
2. **首启声明**：弹"声明"对话框（免费软件/反盗版）→ 点确定。
3. **MANAGE_EXTERNAL_STORAGE**（所有文件管理权限）：否则读不了 `/sdcard` 下脚本。授予：`adb shell cmd appops set com.taobao.idlefish.x MANAGE_EXTERNAL_STORAGE allow`（注意 `appops` 命令不存在，要用 `cmd appops`）。
4. **无障碍服务**：`settings put secure enabled_accessibility_services` 写进去了但系统不绑定（Android 12 安全机制，`Bound services:{}`）。必须走 UI：困鱼内点"去设置" → 无障碍 → 困鱼 → 开"使用困鱼" → 系统弹窗点"允许"。之后 `dumpsys accessibility` 可见 `Bound services:{Service[label=困鱼...]}` 才算成功。
5. **跑脚本**：`adb shell am start --user 0 -a android.intent.action.VIEW -n com.taobao.idlefish.x/org.autojs.autojs.external.open.RunIntentActivity -d "file:///sdcard/xxx.js" -t "text/javascript"`（脚本里 `auto.waitFor()` 等无障碍就绪）。Auto.js 的 `toastLog/console.log` 进应用内控制台、**不进 logcat**；脚本结果用 `files.write("/sdcard/xxx.txt")` 落盘再 `adb pull`。

## 七、⚠️ 重大阻塞："切换为个人版"无法被自动化点击

广场版顶部"切换为个人版"按钮（bounds `Rect(600,140-840,236)`，中心 720,188），试了**全部自动化方式都不切换**：

| 方式 | 结果 |
|---|---|
| `adb shell input tap 720 188` | 不切换（原生标题栏拦截触摸） |
| 困鱼全局 `click(720,188)`（dispatchGesture 手势） | 不切换（同上，手势仍是屏幕坐标触摸） |
| 无障碍 `text("切换为个人版").click()`（ACTION_CLICK） | `ok=true` 但不切换 |
| 无障碍点祖先链（Button + 4 层 View） | 全部 `ok=true`，全不切换 |

**根因**：该 H5 按钮位于原生标题栏正下方（y=140-236），原生层吃掉了所有触摸事件；而无障碍 ACTION_CLICK 派发了但支付宝 WebView 没把该元素的 click 动作暴露出来，所以 H5 的 onclick 不触发。

**影响**：旧脚本要进"个人版"才能走经典收能量流程（找能量、好友树）。若这个按钮点不动，脚本就无法切到个人版。需确认：(1) 真机鼠标/手指实点能否切换（排除是不是 MuMu/自动化特有问题）；(2) 是否有别的入口（深链 appId、设置项）进个人版；(3) 当前版本的收能量流程是否已改在广场版内直接进行（不再需要个人版）。

**补充（用户实测）**：在 MuMu 里用鼠标实点"切换为个人版" → **能成功切到个人版**。所以按钮本身是好的，只是自动化（adb tap / 困鱼手势 / 无障碍 ACTION_CLICK 点 5 层）都点不动它。推测真机用 Auto.js 的 `text("切换为个人版").click()`（无障碍）也可能点不动（同一套支付宝 WebView 行为）——这点需在真机上验证，是脚本的潜在风险点。

## 八、个人版界面调研（部分完成）

切到个人版后（adb 不抢前台的方式抓的），**暴露出经典蚂蚁森林的稳定 H5 元素 ID**，这对脚本是大好事（比固定坐标稳得多）：

| H5 元素 ID | 含义 | 脚本用途 |
|---|---|---|
| `J_treeContainer` (clickable) | 树容器 | 定位树区域 |
| `J_treeCanvas` (Image) | 树画布，**能量球画在上面** | 能量球检测区域的父容器 |
| `J_home_panel` | 首页面板 | - |
| `J_tree_dialog_wrap` | 树上的对话框包裹 | 弹窗定位 |
| `J_appFlow` | 广场社交流（个人版里在 0,0 隐藏） | - |

用户信息区也在（Eric / 钻石59名 / 证书164 / 113天 / 去保护）。

**未完成（个人版视图会自动超时退回支付宝首页，只抓到前 100 个节点）**：
- **"找能量"按钮**未定位到（在 100 号节点之后）——这是逛好友收能量的入口，旧脚本 `StrollScanner` 靠 OCR 找"找能量"，需确认当前个人版里它还在。
- **能量球区域**：J_treeCanvas 是整块画布，能量球在其子区域，旧脚本的 `tree_collect_left/top/width/height` 需按当前分辨率重算（要看到真实能量球才能精确标定）。
- **原生弹窗 ID**（`J_pop_treedialog_close` 等）：还没在个人版里触发弹窗验证。

## 九、修复决策日志（2026-07-16，让老脚本适配新界面）

环境：MuMu emulator-5556（1440×2560，伪装华为）、困鱼、支付宝小号已登录、脚本已推至 `/sdcard/脚本/ant-forest/`。验证手段：`adb pull logs/log-verboses.log` grep（AutoJS debugInfo 不进 logcat，要拉日志文件）。

### 决定1：逛一逛转场改滑动（✅ 已验证）
- `core/StrollScanner.js` collecting() 加 `firstRound` 标志：首轮点找能量按钮入口、后续轮 `automator.randomScrollDown()`。
- 日志铁证："首次入口 click 找能量" + "滑动到下一个好友"×多，节奏 ~14s/好友。

### 决定2：能量球区域根因 = 陈旧 autoDetect 配置（⚠️ 球已通过过滤，但点击未增能量，待查）
- **根因**：config 存储 `tree_collect_*` 被旧次 `autoDetectTreeCollectRegion` 标成 `[236,496,968,150]`（height=150 窄带，y 仅 496-646），新版真球在 y[690,1010]。autoDetect 完成后自设 `auto_detect=false` 永不重算 → 每次读这个"自信地错的值"，霍夫球全被区域过滤排除。
- **临时修复**：`overwrite` 设 `[0,400,1440,700]` + `auto_detect=false`。
- **部分成功**：球 y878/891 通过区域+颜色过滤（mainAvg=87.72），收自己球被点击。**但能量始终 186921 未增**——点击未真收到。怀疑区域偏宽纳入误检球（已收残影/倒计时球颜色相近），或自己今日已无成熟球。
- **永久修复方向**：autoDetect 的 `height=anchorHeight*2.5` 系数太小（算出 150），新版球位更低，需增大或改用 J_treeCanvas bounds。
- **待查**：需截图看点击位置是不是真能量球；收好友场景日志被 main.js 结束清 verbose 丢失，需跑中 pull。

### 决定3：环境坑修复
- **force-stop 破坏无障碍**：`am force-stop` 触发 Android 机制禁用无障碍（"启用但未运行"幽灵态，连带 uiautomator dump null root）。修复：`settings put secure` 彻底 toggle（先空再设回）。**教训：重启脚本用 RunIntentActivity，别 force-stop。**
- **判无障碍运行态**：`uiautomator dump` 是否成功才准（`Bound services` 只是配置层）。
- **悬浮窗权限**：`cmd appops set com.taobao.idlefish.x SYSTEM_ALERT_WINDOW allow`（首跑 FloatyUtil 会卡设置页）。
- **YOLO 当前困鱼不支持**：`Yolo支持：false`（importClass onnxruntime 失败），需换集成 onnxruntime 的运行时。
- **adb 坑**：Git Bash 把 `/sdcard/` 转 Windows 路径（MSYS），用 PowerShell；am start 中文路径 URL 编码。

### 决定4：能量球区域排除右下角误检（✅ 已验证 22:07，收能量打通）
- **发现**：决定2 的临时区域 `[0,400,1440,700]` 太宽，纳入好友页右下角**浇水按钮**（橙色水壶 x1000-1100 y900-950）和**留言按钮**（x950-1050），圆形+亮色骗过霍夫+颜色过滤（mainAvg 95-143），被误当能量球点击 → 点了不收能量。analyze_image 交叉验证：日志球坐标 [1147,1013]/[1170,1003] 与它识别的浇水按钮 x1000-1100 y900-950 严丝合缝。
- **修复**：缩区域到 `[50,300,880,600]`（x50-930 y300-900），排除 x>930 右下角按钮区，覆盖树周围。
- **验证**：新 engine 球坐标回到 x696/817（<930，树周围），不再误点右下角；能量 186921 → **187044（+123g）**，真收到能量。
- **根因链总结**：① 陈旧 autoDetect 配置（height=150 窄带）排除真球 → ② 临时宽区域纳入右下角按钮误检。两步修完，收能量链路通。
- **待办（永久修复）**：`[50,300,880,600]` 是 overwrite 进 storage 的，换分辨率要重算。应改 `autoDetectTreeCollectRegion`：height 系数 `*2.5`→更大；排除右下角（x>device_width*0.65）；或改 config.js 默认值适配 1440×2560。
- **调试方法论**：`analyze_image`（zai MCP）做位置/结构判断 + 日标球坐标交叉验证，可靠；Read 工具对 adb screencap PNG/JPG 持续 Unsupported，弃用。

### 决定5：unit 核对结论（启动链路正常，深度受手动触发约束）
- **森林集市.js**：am start 后启动链路正常（解锁→初始化→"加入任务队列"），卡在 `showCommonDialogAndWait` 对话框（设计，需用户在 MuMu 点确认）；`market.exec`（在 `unit/森林集市/internal.js`）待手动确认后核对。退出时 logcat 有 finalizer Uncaught exception（非阻塞）。
- **神奇物种万能卡.js**：默认 UI 模式（悬浮窗按钮），需手动点"领万能卡"；定时任务模式才自动跑 `doCollectCards`。深链 `appId=68687886`。
- **拼手速.js**：要手动进打地鼠界面 + 手动点"开始"，纯 adb 无法核对。
- **main.js 内辅助**：浇水回馈/双击卡条件性触发（没收超阈值不触发），本次未触发，逻辑存在待条件验证。
- **error.log "未找到奖励按钮"×多**：main.js 的 `BaseScanner.checkForPlantReward` 找赠礼图片失败（未配 `reward_for_plant` 图 + YOLO 不支持），warnInfo 非阻塞。
- **核心 main.js 完全可用**：累积收 **167g**（10 次运行，186921→187088），收自己+逛一逛+计时循环+保护罩检测全通。
- **结论**：unit 启动链路正常，深度功能受设计约束（对话框/按钮/进界面/时机）需手动触发，非 bug。
- **待核对（需手动配合/时机）**：能量雨（开放时段）、自动巡护（当日步数，模拟器可能 0 步）、神奇海洋（需关3D）、森林寻宝（互助码）、多账号（需配置）。
- **已知小 bug（非阻塞）**：① `getCurrentEnergy` 偶尔读 0（控件加载时机）；② checkForPlantReward 找赠礼图失败（配置/YOLO 问题）。
- **永久修复待办**：`autoDetectTreeCollectRegion` height 系数 `*2.5`→更大、排除右下角（x>device_width*0.65）、适配多分辨率；getCurrentEnergy 读取加重试。

### 决定6：unit 核对钥匙 `limit_runnable_time_range` + 森林集市核对（2026-07-17 凌晨）
- **关键发现**：unit 深夜（0:30-6:50）直接 exit **不是 bug**，是 `ProjectCommonFunctions.inLimitTimeRange`（`!develop_mode && limit_runnable_time_range && 0:30≤时间≤6:50`，460-467 行）。`develop_mode` 的 overwrite **不跨 engine 生效**（该键不从 storage 加载），但 `limit_runnable_time_range=false` 的 overwrite 生效——**这是深夜核对所有 unit 的钥匙**。之前所有 unit 启动即退出都卡这。
- **森林集市.js 核对**（设 limit=false 后，E:12）：✅ 越过限制 → `market.exec` → 集市界面识别"浏览商品15s得能量"按钮正常 → 进入浏览商品流程。⚠️ 浏览后找"任务已完成.*立即领取"反复超时（领取环节，新版"立即领取"按钮可能变化，待适配 `unit/森林集市/internal.js`）。
- **结论修正**：unit 不是"启动链路正常就够"——要越过限制时段才能跑到业务逻辑。forest 集市核心（进集市+浏览）通，领取环节有新版适配点。
- **核对方法论**：深夜核对 unit 前，先跑 `config.overwrite("limit_runnable_time_range", false)`，否则 unit 全在 showDialogAndWait 的 inLimitTimeRange 检查处 exit。

### 决定7：批量 unit 核对（forest 集市/寻宝/巡护，2026-07-17）
- **方法论**：`limit_runnable_time_range=false`（深夜钥匙）+ `engines.all().forceStop()`（清 Dispatcher 队列，一次一个 unit）+ am start unit + 看 log-verboses。
- **森林集市.js**（E:12）：✅ 进 `market.exec` + 集市界面识别"浏览商品15s得能量"。⚠️ 浏览后找"任务已完成.*立即领取"超时（领取适配点）。
- **森林寻宝.js**（E:18）：✅ 进支付宝寻宝界面 + 跑流程。⚠️ 找"签到/领取/抽奖按钮"超时。
- **自动巡护.js**（E:19）：✅ 进支付宝 + 跑巡护流程。⚠️ 找"兑换巡护机会|开始巡护"超时（可能模拟器步数 0）。
- **规律**：unit 启动 + 进对应界面**通**，但**具体功能按钮找不到**——新版按钮文本/位置变（适配点）or 当日已完成/无前置（步数0 等）。**纯日志无法区分两者**，需截图对比新版 UI 确认按钮在不在、文本是什么。
- **待核对**：神奇海洋（需关3D）、神奇物种（UI 模式按钮）、能量雨（需进界面+手动开始）、多账号（需配置）、拼手速（纯手动）。
- **适配点修复**（需截图对比后改选择器）：forest 集市 internal.js 的"立即领取"、寻宝的"签到/领取/抽奖"、巡护的"兑换巡护机会"——这些"找不到"如确认是新版文本变化，改 `widgetUtils.widgetGetOne` 的正则即可；如是时机（已完成/步数0），脚本本身没坏。

### 决定8：Shizuku 配置受阻 + 环境硬约束（2026-07-17）
- Shizuku v13.6.0 装成功，但启动受阻：① **MuMu 无 root**（`su: inaccessible or not found`，adb shell 仅 uid=2000）；② **v13.6.0 不再创建外部 `start.sh`**（`/sdcard/Android/data/moe.shizuku.privileged.api/` 目录不存在），改用 `/data/local/tmp/shizuku_starter`（需从 app 内复制，但 app UI"通过连接电脑启动"`click=false` 不展开，拿不到 starter 命令）；③ 无线调试启动要系统开无线调试+配对（多步 UI）。
- **更关键的硬约束**：即使 Shizuku 配齐（解点击），**神奇海洋领奖励仍卡 YOLO**（error: "领奖励需要YOLO支持"）。困鱼运行时**无 onnxruntime**（决定3），所有 YOLO 依赖功能（神奇海洋领奖励、YOLO 能量球、一键收 YOLO 识别）都不可用——除非换集成 onnxruntime 的运行时（新版 AutoJS Modify）。
- **结论**：Shizuku 在当前 MuMu 性价比低（曲折 + 不解 YOLO）。神奇海洋领奖励要 YOLO，是运行时能力上限，非脚本可修。

### 决定9：YOLO 是免费 AutoJS 生态上限（2026-07-17，最终结论）
- 试换运行时：TonyJiangWJ/Auto.js **4.1.25.0702** FakeIdlefish（2025-07 latest）覆盖安装——仍 `importClass(ai.onnxruntime.OnnxTensor)` 失败（"ai" is not defined）；Ant-Forest v1.1.1.4 的 AutoJS.fake（2024-12 更旧，同代码库）更不可能集成。**都不支持 YOLO**。
- web 调研确认：**YOLO ONNX 推理是 AutoJS Pro 的插件**（商业版），免费 Modify/困鱼不集成 onnxruntime。README"最新版修改版支持 YOLO"是误导（实际要 Pro + ONNX 插件，且 README 自己不推荐 Pro）。
- **最终结论**：YOLO 依赖功能（神奇海洋领奖励、YOLO 能量球识别、一键收 YOLO）在**免费运行时不可用**，是生态硬约束。**"全部功能可用"在免费 AutoJS 下不可能**（YOLO 部分受 Pro 限制）。
- 4.1.25 FakeIdlefish 覆盖安装保留了配置（`tree_collect_*`/`limit_runnable_time_range`）+ 授权（无障碍/悬浮窗）+ 脚本，**核心 main.js 功能照常**（非 YOLO 路径不受影响）。
- **可达成的"全部可用"**（免费运行时）：核心收能量+逛一逛（167g 验证）+ 非 YOLO unit（forest 集市/寻宝/巡护核心通，按钮适配点待截图对比修）+ 非 YOLO 辅助（能量雨/浇水/双击卡/多账号）。**不可达**：YOLO 依赖（神奇海洋领奖励/YOLO 能量球/一键收 YOLO）——除非上 Pro。

### 决定10：unit 适配点真相——活动/交互整体变更（非按钮文本变，2026-07-17）
- 原假设"unit 按钮找不到=文本变，改正则即可"**被截图推翻**：
  - **forest 集市**（mkt.png analyze）：新版是"天猫森林集市"电商商品页，交互"再点击3次即可获得15g能量"（点击商品），非旧版"浏览商品15s→任务完成立即领取"。`browseAds` 不匹配。修复=重写 `BrowserExecutor`。
  - **森林寻宝**（xb.png analyze）：寻宝.js **没进寻宝界面**，停在天猫森林集市商品页（与 forest 集市同页）。寻宝活动可能下线（9周年过期，update_info 提"9周年活动"）/整合到森林集市/深链 appId 失效。找"签到/领取/抽奖"超时=没进真寻宝界面。
- **结论**：unit 适配点是**活动/交互模式整体变更**（脚本针对旧活动），不是简单按钮文本变。修复要重写 unit 逻辑（大工程），且部分活动可能已失效（无法修）。**性价比低**（低频 unit + 活动会再变）。
- **稳定可用的是核心**：收能量+逛一逛是蚂蚁森林基础功能（不随活动变），167g 验证。活动类 unit（集市/寻宝）随支付宝运营变更，脚本追赶成本高、价值低。

### 决定11：unit 核对最终总结（全部完成，2026-07-17）
- 全部 unit 核对完毕，按约束分类：
  - **活动/交互变更**（需重写逻辑）：forest 集市（浏览→点击商品）、森林寻宝（活动下线/整合，没进寻宝界面停在天猫森林集市）
  - **环境硬约束**（需 Pro/Shizuku）：神奇海洋（领奖励需 YOLO、点击需 Shizuku）
  - **UI 模式/手动触发**（设计如此，非 bug）：神奇物种万能卡（悬浮窗按钮）、能量雨（进界面+手动开始）、拼手速（进打地鼠+手动开始）
  - **配置依赖**：多账号（需提供多账号）
  - **时机依赖**：自动巡护（模拟器无真实步数）
- **结论**：unit"全部可用"受 **5 重约束**（活动变更 / UI模式 / 环境 / 配置 / 时机），大多非脚本能简单修。核心 main.js（收能量+逛一逛+计时+保护罩）是**稳定可用**的（167g），不受这些约束影响。
- **建议**：核心够用即收工。unit 深度（重写活动类 / 上 Pro+Shizuku / 配多账号）按真实需求后续推进，不在当前核对范围。
